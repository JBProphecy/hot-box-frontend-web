////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

import { forwardRef } from "react"
import type { ElementAttributes } from "@/types"
import { Div } from "./vanilla"

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export const Wrapper = forwardRef<HTMLDivElement, ElementAttributes>((props, ref) => {
  const { classes = [], ...attributes } = props
  return <Div ref={ref} classes={["wrapper", ...classes]} {...attributes} />
})

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
